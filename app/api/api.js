var moment     = require('./moment/momentRoute');
var device     = require('./device/deviceRoute');
var test     = require('./testRoute');

module.exports = {
    moment:moment,
    device:device,
    test:test
}